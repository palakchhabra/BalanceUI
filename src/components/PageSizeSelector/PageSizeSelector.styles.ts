import type { CSSProperties } from "react";

export const selectorStyle: CSSProperties = {
  height: 32,
  padding: "0 8px",
  borderRadius: "var(--bu-radius-sm)",
  border: "1px solid var(--bu-border)",
  background: "var(--bu-surface)",
  color: "var(--bu-fg)",
  fontSize: 13,
};
